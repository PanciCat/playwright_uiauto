[pytest]
addopts = -q -ra --strict-markers --tb=short --alluredir=allure-results
testpaths = tests
filterwarnings =
    ignore::pytest.PytestAssertRewriteWarning

markers =
    smoke: quick checks / 冒烟用例
    regression: full suite / 回归用例
